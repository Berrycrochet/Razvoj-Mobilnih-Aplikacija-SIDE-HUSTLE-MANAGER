<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Side_Hustle_Manager.Pages.Admin.AdminDashboardPage"
             Title="AdminDashboardPage">


    <ScrollView>
        <VerticalStackLayout Padding="10" Spacing="10">

            <Picker x:Name="CategoryPicker"
                    Title="Kategorija"
                    SelectedIndexChanged="FilterChanged"/>

            <Picker x:Name="EmployerPicker"
                    Title="Poslodavac"
                    SelectedIndexChanged="FilterChanged"/>

            <CollectionView ItemsSource="{Binding SideHustles}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border BackgroundColor="#FFFFFF"
                                StrokeShape="RoundRectangle 12"
                                Padding="10"
                                Margin="6">

                            <VerticalStackLayout Spacing="8">

                                <Image Source="{Binding ImagePath}"
                                       HeightRequest="140"
                                       Aspect="AspectFill"/>

                                <Label Text="{Binding Title}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="#1E2D24"/>

                                <Label Text="{Binding Description}"
                                       FontSize="14"
                                       TextColor="#6B7F74"/>

                                <Label Text="{Binding PayDisplay}"
                                       FontAttributes="Bold"
                                       TextColor="#27AE60"/>

                                <Label Text="{Binding EmployerName}"
                                       FontSize="12"
                                       TextColor="#6B7F74"/>

                                <!-- VRATI HORIZONTAL STACK SA DUGMIĆIMA -->
                                <HorizontalStackLayout Spacing="10" Margin="0,10,0,0">
                                    <Button Text="Uredi"
                                            BackgroundColor="#2ECC71"
                                            TextColor="White"
                                            Clicked="Edit_Clicked"
                                            CommandParameter="{Binding .}"/>

                                    <Button Text="Izbriši"
                                            BackgroundColor="#E74C3C"
                                            TextColor="White"
                                            Clicked="Delete_Clicked"
                                            CommandParameter="{Binding .}"/>

                                    <Button Text="Prijave"
                                            BackgroundColor="#27AE60"
                                            TextColor="White"
                                            Clicked="Applications_Clicked"
                                            CommandParameter="{Binding .}"/>
                                </HorizontalStackLayout>

                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>